# If Phi3 isn't actually available as a standard .mlpackage, let's verify what happens 
# with optimum exporter. We tried this earlier and it failed because optimum requires extra 
# dependencies for coreml. Let's see if we can install coreml export support.
